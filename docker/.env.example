# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 应用启动参数（可选）
# 例如：ARGS=--port 3000 --api-key your-key
ARGS=

# Go 代理设置（用于构建 TLS sidecar）
GOPROXY=https://goproxy.cn,direct
GOSUMDB=sum.golang.org

# Docker 构建时的代理设置（可选）
DOCKER_HTTP_PROXY=
DOCKER_HTTPS_PROXY=
DOCKER_NO_PROXY=localhost,127.0.0.1
